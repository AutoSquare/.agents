---
name: handoff
description: 将会话紧凑整理成交接文档，供另一智能体续作。
argument-hint: "下一会话主要用于什么？"
---

撰写交接文档汇总当前会话，使新会话中的智能体能接续工作。保存路径由 `mktemp -t handoff-XXXXXX.md` 生成（写入前先读取该路径文件）。

若有合适技能，向下一会话建议要使用哪些。

勿重复已在其它工件中捕获的内容（PRD、计划、ADR、工单、提交、diff）。改以路径或 URL 引用。

若用户传入参数，将其视作下一会话聚焦方向的描述并据此调整内容。
